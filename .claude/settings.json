{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "cd '/Users/emilio/Desktop/Projects with Claude/Maple/maplecommunity.org' && git add -A && git diff --cached --quiet || (git commit -m 'Auto-update from Claude Code' && git push) 2>/dev/null || true",
            "statusMessage": "Auto-committing and pushing..."
          }
        ]
      }
    ]
  }
}
